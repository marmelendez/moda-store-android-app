<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.bedu.modastoreapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OrderActivity"></activity>
        <activity android:name=".ProfileActivity" />
        <activity
            android:name=".ProductActivity"
            android:exported="true" />
        <activity
            android:name=".SearchProductActivity"
            android:exported="true" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".ConfigAddressActivity" />
        <activity android:name=".ConfigPaymentActivity" />
        <activity android:name=".ConfigurationActivity" />
        <activity android:name=".ConfigAccountDataActivity" />
        <activity android:name=".LogInActivity" />
        <activity android:name=".ShopActivity" />
        <activity
            android:name=".CartActivity"
            tools:ignore="Instantiatable" />
        <activity android:name=".DetailActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>

    <!--android:theme="@style/Theme.ModaStoreApp">
            <activity android:name=".Orders"></activity>
            <activity android:name=".ProfileActivity" />-->